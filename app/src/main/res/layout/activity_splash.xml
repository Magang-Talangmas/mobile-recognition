<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_mesh_premium">

    <!-- Center Container -->
    <FrameLayout
        android:id="@+id/flLogoContainer"
        android:layout_width="180dp"
        android:layout_height="180dp"
        app:layout_constraintBottom_toTopOf="@+id/pbSplashLoading"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        android:layout_marginBottom="32dp">

        <!-- Outer Glowing Ring -->
        <View
            android:id="@+id/vGlowRing"
            android:layout_width="160dp"
            android:layout_height="160dp"
            android:layout_gravity="center"
            android:background="@drawable/bg_circle_primary"
            android:backgroundTint="#1A00E5FF"
            android:alpha="0" />

        <!-- Main Logo -->
        <ImageView
            android:id="@+id/ivSplashLogo"
            android:layout_width="130dp"
            android:layout_height="130dp"
            android:layout_gravity="center"
            android:src="@drawable/logo_face_recognition"
            android:alpha="0"/>

        <!-- Laser Scanning Beam -->
        <View
            android:id="@+id/vScanLine"
            android:layout_width="140dp"
            android:layout_height="4dp"
            android:layout_gravity="center"
            android:background="@drawable/bg_laser_line_cyan"
            android:alpha="0" />
    </FrameLayout>

    <!-- Custom Styled Progress Bar -->
    <ProgressBar
        android:id="@+id/pbSplashLoading"
        style="?android:attr/progressBarStyle"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:indeterminate="true"
        android:indeterminateTint="#00E5FF"
        app:layout_constraintTop_toBottomOf="@id/flLogoContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:alpha="0"/>

</androidx.constraintlayout.widget.ConstraintLayout>
